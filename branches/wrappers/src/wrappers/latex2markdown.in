#!/bin/sh -e
# runs pandoc to convert latex to markdown

THIS_NARG=1

### common.sh

### getopts.sh

### postopts.sh

### checkin.sh

to_utf8 "$@" | pandoc $PANDOC_OPTS -r latex -w markdown -s | from_utf8
